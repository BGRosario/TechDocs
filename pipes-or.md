# Pipes |

The shell uses a pipe to connect the output of one command directly to the input of another command

The symbol for a pipe is the vertical bar ( | ). The command syntax is:

```
command1 [arguments] | command2 [arguments]
```

